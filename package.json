{
  "name": "oil-spread-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hyperliquid Brent / WTI dashboard and persistent Feishu threshold monitor",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.mjs",
    "test": "node --test",
    "check": "node scripts/check.mjs"
  }
}
